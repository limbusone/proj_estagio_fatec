<h1>Lista de Convênios</h1>

<table>
  <tr>
    <th>Empresa</th>
    <th>Interveniente</th>
    <th>Número de vigência</th>
    <th>Rescidido</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @convenios.each do |convenio| %>
  <tr>
    <td><%= convenio.contratante.nome %></td>
    <td><%= convenio.interveniente.nome %></td>
    <td><%= convenio.vigencia %></td>
    <td><%= convenio.rescidido %></td>
    <td><%= link_to 'Show', convenio %></td>
    <td><%= link_to 'Edit', edit_convenio_path(convenio) %></td>
    <td><%= link_to 'Destroy', convenio, confirm: 'Tem certeza?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Novo convênio', new_convenio_path %>
