<h1>Lista de Alunos</h1>

<table>
  <tr>
    <th>Curso</th>
    <th>Nome</th>
    <th>Número de matricula</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @alunos.each do |aluno| %>
  <tr>
    <td><%= aluno.curso.nome %></td>
    <td><%= aluno.nome %></td>
    <td><%= aluno.num_matricula %></td>
    <td><%= link_to 'Ver', aluno %></td>
    <td><%= link_to 'Editar', edit_aluno_path(aluno) %></td>
    <td><%= link_to 'Deletar', aluno, confirm: 'Tem certeza?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Novo Aluno', new_aluno_path %>
